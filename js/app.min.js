"use strict";

var mainMenu = document.querySelector(".main-nav__wrapper"), toggleBtn = document.querySelector(".main-nav__toggle"), closeBtn = document.querySelector(".main-nav__close");

function toggleMenu() {
    mainMenu.classList.toggle("hide-menu"), toggleBtn.classList.toggle("hide-menu"), 
    closeBtn.classList.toggle("hide-menu");
}

closeBtn.addEventListener("click", toggleMenu), toggleBtn.addEventListener("click", toggleMenu), 
mainMenu.classList.add("hide-menu"), closeBtn.classList.add("hide-menu"), toggleBtn.classList.remove("hide-menu");